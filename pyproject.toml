[project]
name = "lc-simulator"
version = "0.1.0"
description = "Local Python-only LeetCode Hot 100 CLI simulator"
requires-python = ">=3.9"

[project.scripts]
lc = "lc.cli:main"

[tool.setuptools]
packages = ["lc"]

[tool.pytest.ini_options]
testpaths = ["tests"]
